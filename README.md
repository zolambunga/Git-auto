# Git-auto
git-auto automatiza git add, commit e push, gerando mensagens de commit com IA a partir do git diff. É um wrapper Bash opinativo, focado em produtividade pessoal, não em portabilidade ou segurança máxima.
